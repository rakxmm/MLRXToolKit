cmake_minimum_required(VERSION 3.19)

set(CMAKE_EXPORT_COMPILE_COMMANDS ON)

project(libalg
    VERSION     0.0.1
    DESCRIPTION "Library that represents matrices and its operations."
    LANGUAGES   CXX
)

# Skopíruje priečinok data do build priečinka pri každej zmene
file(COPY ${CMAKE_CURRENT_SOURCE_DIR}/data DESTINATION ${CMAKE_CURRENT_BINARY_DIR}/src)

add_subdirectory(src)